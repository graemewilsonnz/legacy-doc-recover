# Input documents

Place local `.doc` files here when testing recovery. Files in this directory are
ignored by Git so private documents are not published; only this README is kept.

Example:

```bash
legacy-doc-recover input/damaged.doc -o output/damaged.recovered.txt --report output/damaged.recovery.json
```
