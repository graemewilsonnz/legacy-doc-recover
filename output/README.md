# Recovery output

Generated recovery files can be written here so they are easy to find:

```bash
legacy-doc-recover damaged.doc -o output/damaged.recovered.txt --report output/damaged.recovery.json
```

Recovered text and JSON reports in this directory are ignored by Git. Review
them locally and do not publish sensitive document content.
